<div class="with-navbar">
<div class="navbar" style="background-color: #eef4f9;">
<div class="navbar-content container-fluid d-flex justify-content-between align-items-center">
    <%= link_to "root_path", class: "navbar-brand" do %>
      <%= cl_image_tag "Melo_logo_muj2f6" %>
    <% end %>


      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto">
          <% if user_signed_in? %>
            <li class="nav-item active d-flex align-items-center">
              <%= link_to "", class: "btn" do %>
                <i class="fa-solid fa-bars"></i>
              <% end %>
            </li>
        <% else %>
          <li class="nav-item active d-flex align-items-center">
            <%= link_to "Login", new_user_session_path, class: "btn btn-success btn-rounded-sm" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
  </div>
  </div>
</div>
